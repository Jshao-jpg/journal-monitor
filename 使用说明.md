# ⚠️  如何使用系统

## 🔍 当前问题诊断

根据系统记录，最近成功登录但未找到稿件（16:03:37）。

可能的原因：
1. **用户名/密码未在Web界面输入** - 导致使用了空的环境变量
2. **登录成功但未找到"Revisions Being Processed"链接** 
3. **页面结构变化导致提取失败**

## ✅ 正确使用步骤

### 步骤1: 确保服务器正在运行

```bash
# 在项目目录执行
npm start
```

应该看到：
```
🚀 论文状态监控系统已启动！
📱 访问地址: http://localhost:3000
⏰ 检查频率: 每1小时
```

### 步骤2: 打开Web界面

在浏览器中访问: **http://localhost:3000**

### 步骤3: 输入登录信息

在Web界面中输入：
- **Editorial Manager 用户名**: `zhenghua@dgut.edu.cn`（您的实际邮箱）
- **Editorial Manager 密码**: 您的密码

⚠️ **重要**: 必须在Web界面手动输入，不要依赖.env文件！

### 步骤4: 点击"测试连接"

点击后，系统会：
1. 启动Puppeteer浏览器
2. 访问登录页面
3. 在iframe中输入您的用户名和密码
4. 点击"Author Login"按钮
5. 等待登录完成
6. 查找并点击"Revisions Being Processed"链接
7. 提取稿件信息

### 步骤5: 查看结果

如果成功，应该看到：
```
✅ 测试成功
找到 1 篇稿件：
- JOLT-D-25-03577R1: Under Review
```

## 🐛 如果显示"未找到稿件"

### 方法1: 查看详细日志

打开浏览器开发者工具（F12），查看Console标签页，会显示详细的操作日志。

### 方法2: 查看截图

系统会保存以下截图到 `data/` 目录：
- `login_page.png` - 登录页面
- `main_menu.png` - 登录后的主菜单
- `revisions_page.png` - 稿件列表页面

检查这些截图看看是否成功登录和导航。

### 方法3: 运行诊断脚本

```bash
node diagnose.js
```

会显示最近的检查记录和配置状态。

## 📝 测试用例

为了确保系统正常工作，您可以：

1. **打开浏览器手动登录** Editorial Manager
2. **导航到 "Revisions Being Processed"**
3. **查看页面源代码**，确认表格结构

如果手动能看到稿件，但系统提取不到，请截图页面并提供给我分析。

## 🔧 手动测试登录

您也可以直接运行测试脚本（需要在.env中设置用户名和密码）：

```bash
# 1. 编辑 .env 文件
# 设置:
# EM_USERNAME=zhenghua@dgut.edu.cn  
# EM_PASSWORD=你的密码

# 2. 运行测试
node -e "const m=require('./services/monitor'); m.checkStatus(process.env.EM_USERNAME, process.env.EM_PASSWORD).then(r=>console.log(JSON.stringify(r,null,2)));"
```

这将显示详细的操作日志和结果。

## 📞 下一步

如果按照上述步骤操作后仍然显示"未找到稿件"，请提供：
1. 浏览器控制台的完整日志
2. `data/` 目录中的所有截图
3. 手动登录后看到的稿件列表截图

我将进一步优化代码！
